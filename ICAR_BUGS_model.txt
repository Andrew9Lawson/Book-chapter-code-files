model{
    for(i in 1:m){
    Y1998[i]~dpois(mu[i])
    mu[i]<-Exp98[i]*theta[i]
    log(theta[i])<-a0+u[i]
    }
u[1:m]~car.normal(adj[],wei[],num[],tauU)
for(k in 1:sumNumNeigh) {
      wei[k] <- 1 }

 a0~dnorm(0,tau0)
tau0~dgamma(2,0.5)
tauU~dgamma(2,0.5)   
    }